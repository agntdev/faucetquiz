[
  {
    "name": "My progress opens from its menu button",
    "steps": [
      {
        "send": { "callback": "profile:show" },
        "expect": [{ "method": "editMessageText", "payload": { "text": "Your rewards profile is getting connected. Come back soon!" } }]
      }
    ]
  }
]
